[
    if(
        or(
            empty(
                steps('projectConfig').prefix
            ), 
            empty(
                steps('projectConfig').project
            ), 
            empty(
                steps('projectConfig').environment
            )
        ),
        '',
        concat(
            steps('projectConfig').prefix, 
            '-', 
            steps('projectConfig').project, 
            '-', 
            steps('projectConfig').environment, 
            '-' ,'modules-asp'
        )
    )
]